# NameID replacer dependencies
# Scripts for manipulating NameID table entries in font files.

# Core dependencies (required by all scripts)
fonttools>=4.40.0
rich>=13.0.0

# No additional dependencies beyond core
